// import TrackList from './TrackList';
import {shallow} from 'enzyme';

describe('TrackList', 
	() => {
		// it('show 2 tracks', () => 
		// 		{
		// 			const props = { tracks: [{id:1 , title: 'track 1 here'}, {id:2, title: 'track 2 here'}] };
		// 			const element = shallow(<TrackList {...props} />);
		// 			expect(element.find('div > div')).to.have.length(2);
		// 		}
		// 	);

		// it('right track', () => 
		// 		{
		// 			const props = {tracks: [{id:3, title:'right track'}]};
		// 			const element = shallow(<TrackList {...props} />);
		// 			expect(element.contains('right track')).to.be.true;
		// 		}
		// 	);

});
